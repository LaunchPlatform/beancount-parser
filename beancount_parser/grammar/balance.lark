BALANCE_PREFIX: DATE _WS+ "balance"
balance: BALANCE_PREFIX ACCOUNT AMOUNT [COMMENT]

%import common.WS_INLINE -> _WS

%import .comment.COMMENT
%import .account.ACCOUNT
%import .amount.AMOUNT
%import .date.DATE
