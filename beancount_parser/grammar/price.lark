PRICE_PREFIX: DATE _WS+ "price"
price: PRICE_PREFIX CURRENCY AMOUNT [COMMENT]

%import common.ESCAPED_STRING
%import common.WS_INLINE -> _WS

%import .comment.COMMENT
%import .currency.CURRENCY
%import .amount.AMOUNT
%import .date.DATE
